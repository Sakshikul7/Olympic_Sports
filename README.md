# Olympic_Sports
The code in the provided notebook starts by importing several key libraries: pandas for data manipulation, numpy for numerical operations, matplotlib.pyplot and seaborn for data visualization, and sklearn.model_selection for data splitting.

The dataset is loaded from an Excel file using pandas.read_excel. The file appears to contain sales data related to various subsidiaries, years, months, sport product categories, sales values, and volumes. The dataset is previewed using df.head(5), which shows the first five rows of the data, highlighting details like country, year, product category, and corresponding sales statistics.

This initial portion of the code focuses on importing the necessary libraries, loading the dataset, and performing an initial exploration of the data to understand its structure
